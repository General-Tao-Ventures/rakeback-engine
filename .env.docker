# Docker environment — local testing with sample data
# Copy into container or mount as env_file in docker-compose

# Database (SQLite, relative to backend/)
DB_SQLITE_PATH=data/rakeback.db

# Chain
CHAIN_RPC_URL=ws://185.189.45.20:9944

# Backend
RAKEBACK_ENVIRONMENT=local
RAKEBACK_DEBUG=true

# Seed sample data on startup (set to "true" for Docker)
SEED_SAMPLE_DATA=true

# Frontend env vars (passed via docker-compose environment)
VITE_TAOSTATS_API_KEY=
VITE_RPC_NODE_URL=
VITE_BACKEND_API_URL=
