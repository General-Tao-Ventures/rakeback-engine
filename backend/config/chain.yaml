# Chain RPC Configuration
# Copy this file to chain.yaml and adjust for your environment

# Bittensor mainnet – use archive node for full state (required for historical blocks)
# Your archive node (no API key):
rpc_url: ws://185.189.45.20:9944

# Alternatives (override with CHAIN_RPC_URL in .env):
# rpc_url: wss://archive.chain.opentensor.ai:443
# rpc_url: wss://entrypoint-finney.opentensor.ai:443  # Official (pruned)
# rpc_url: wss://bittensor-finney.api.onfinality.io/public-ws
# rpc_url: ws://localhost:9944  # Local node

# Request settings
rpc_timeout: 60  # seconds
retry_attempts: 5
retry_delay: 2.0  # seconds

# Finality settings
# Number of blocks to wait before considering a block final
finality_depth: 6
