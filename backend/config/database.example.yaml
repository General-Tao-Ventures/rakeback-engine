# Database Configuration
# Copy this file to database.yaml and adjust for your environment

# PostgreSQL (production)
driver: postgresql+psycopg2
host: localhost
port: 5432
name: rakeback
user: rakeback
password: changeme

# Connection pool settings
pool_size: 5
pool_timeout: 30
pool_recycle: 1800

# SQLite (development only)
# Uncomment to use SQLite instead of PostgreSQL
# sqlite_path: data/rakeback.db
